{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build"
  },
  "devDependencies": {
    "@rocketseat/eslint-config": "^2.1.0",
    "eslint": "^8.46.0",
    "turbo": "^1.5.5"
  },
  "comments": [
    "This is a array of comments to json files",
    "--parallel flag in script => todos os scripts de dev serão executados em paralelo"
  ]
}
